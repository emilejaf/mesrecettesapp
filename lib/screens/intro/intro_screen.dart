import 'package:flutter/material.dart';
import 'package:mesrecettes/screens/intro/components/body.dart';

class IntroScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Body(),
    );
  }
}
